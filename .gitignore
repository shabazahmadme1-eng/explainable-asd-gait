# --- dependencies / build ---
node_modules/
dist/
build/
.vite/

# --- python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
catboost_info/

# --- raw datasets ---
# Skeleton/gait recordings, including the private paediatric deployment cohort,
# are NOT published (identifiable child data). Trained model weights are kept.
colab/*.npz
colab/*.npy
colab/*.pt
colab/*.zip

# --- logs / os / editor ---
*.log
.DS_Store
Thumbs.db
.idea/
.vscode/

# --- manuscript + paper-generation tooling (kept OUT of the public repo) ---
# The manuscript is unpublished and co-authored; the edit_docx*/regen_* scripts
# embed its text/figures and use machine-local absolute paths.
*.docx
*.pdf
colab/edit_docx*.py
colab/number_eqs.py
colab/swap_figs.py
colab/regen_fig*.py
colab/render_eqs.py
